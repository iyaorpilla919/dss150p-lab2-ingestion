source_name: "rest_api_events"
source_type: "Paginated REST API"
location_or_endpoint: "http://127.0.0.1:8000/api/events"
owner: "TBD - data owner placeholder"
description: "Event stream with pagination and updatable records"
record_count: 122
schema:
  - name: event_id
    type: string
  - name: updated_at
    type: timestamp
  - name: amount
    type: number
  - name: metadata
    type: object (nested: channel, campaign)
primary_or_candidate_key: "event_id"
update_pattern: "Incremental via updated_after watermark"
known_quality_issues:
  - "Repeated event_id values with newer updated_at timestamps"
acquisition_method: "Paginated GET, deduplicated on ingest"
validation_checks:
  - "event_id not null"
  - "updated_at parseable timestamp"
  - "one logical record per event_id"